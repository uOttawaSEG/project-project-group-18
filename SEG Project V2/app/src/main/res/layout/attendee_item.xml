<?xml version="1.0" encoding="utf-8"?>


<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- TextView for Attendee Email -->


    <TextView
        android:id="@+id/textview_attendeeEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="Attendee Email"
        android:textColor="@android:color/black"
        android:textSize="18dp" />

    <!-- Horizontal LinearLayout for the buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start"
        android:weightSum="3">

        <!-- Button to show details of the attendee -->
        <Button
            android:id="@+id/button_details"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:backgroundTint="#3F51B5"
            android:text="Details"
            android:textSize="16dp" />

        <!-- Button to approve the attendee -->
        <Button
            android:id="@+id/button_approve"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:backgroundTint="#3F51B5"
            android:text="Approve"
            android:textSize="16dp" />

        <!-- Button to reject the attendee -->
        <Button
            android:id="@+id/button_reject"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:backgroundTint="#3F51B5"
            android:text="Reject"
            android:textSize="16dp" />

    </LinearLayout>


</LinearLayout>
